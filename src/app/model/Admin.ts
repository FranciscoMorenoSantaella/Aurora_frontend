/**
 * Es el objeto de administrador que es el encargado de crear productos //Este objeto no esta implementado por falta de tiempo en la vista
 */
export interface Admin {
  id: number,
  name: String,
  email:String,
}